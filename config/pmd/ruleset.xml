<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="speedrunner"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0
                             https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

  <description>
    PMD rules for speedrunner. PMD targets bad patterns, code smells and real
    bugs; pure formatting/style is left to Checkstyle (google_checks) and
    Spotless (google-java-format) to avoid duplicate, conflicting reports.
  </description>

  <!-- SystemPrintln stays enabled: all output goes through Log4J2, never System.out. -->
  <rule ref="category/java/bestpractices.xml"/>

  <rule ref="category/java/errorprone.xml">
    <!-- Reconfigured below to treat 0/0L as non-magic in guard clauses. -->
    <exclude name="AvoidLiteralsInIfCondition"/>
  </rule>
  <rule ref="category/java/errorprone.xml/AvoidLiteralsInIfCondition">
    <properties>
      <!-- Comparing against 0 / 0L in a guard clause is idiomatic, not a smell. -->
      <property name="ignoreMagicNumbers" value="-1,0,0L"/>
    </properties>
  </rule>

  <rule ref="category/java/multithreading.xml">
    <!-- Single-threaded CLI: maps are thread-confined (and are serialization DTOs),
         so ConcurrentHashMap would only add misleading overhead. The remaining
         multithreading checks stay enabled as a safety net. -->
    <exclude name="UseConcurrentHashMap"/>
  </rule>
  <rule ref="category/java/performance.xml"/>

</ruleset>
